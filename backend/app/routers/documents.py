"""Document upload / listing / deletion routes (implemented in Phase 3)."""
from fastapi import APIRouter

router = APIRouter(prefix="/api/documents", tags=["documents"])

# Endpoints are implemented in Phase 3.
