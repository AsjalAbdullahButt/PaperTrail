"""Single AI service module.

All embedding + chat-completion calls live here so the provider can be
swapped by editing only this file. Implemented in Phase 3 (embeddings) and
Phase 4 (generation).
"""
# Implemented in later phases.
